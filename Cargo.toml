[package]
name = "rda5807m"
version = "0.1.0"
edition = "2021"

[dependencies]
embedded-hal = "1.0.0"

[dev-dependencies]
embedded-hal-mock = { version = "0.10", default-features = false, features = ["eh1"] }